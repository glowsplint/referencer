{
  "name": "referencer",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n collab,backend,frontend -c blue,green,yellow \"cd collab-server && fnm exec --using=22 node server.mjs\" \"cd backend && bun run --hot src/index.ts\" \"cd frontend && bun ./node_modules/vite/bin/vite.js\"",
    "install:all": "bun install && cd collab-server && bun install && cd ../backend && bun install && cd ../frontend && bun install",
    "format": "bunx prettier --write .",
    "format:check": "bunx prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

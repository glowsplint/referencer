{
  "name": "referencer",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n collab,backend,frontend -c blue,green,yellow \"cd collab-server && node server.mjs\" \"cd backend && bun run --hot src/index.ts\" \"cd frontend && bun run vite\"",
    "install:all": "cd collab-server && npm install && cd ../backend && bun install && cd ../frontend && bun install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
